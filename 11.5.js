

  const myltiply = (x,n) =>{
  const result = (x**n);
  

  return result;
}
  let x = prompt("введите число", '');
  let n = prompt("введите степень", '');

console.log(myltiply(3,2));  //1-вариант.
alert(myltiply(x,n));    //2-вариант.

